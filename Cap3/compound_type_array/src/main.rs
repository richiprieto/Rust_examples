fn main() {
    //un array debe ser del mismo tipo todos los elementos
    // es de tama;o fijo igual que la tupla
    //array es util cuando se desea que los datos se asignen al stack en lugar de heap
    let a = [1, 2, 3, 4, 5];
    let indice = 0;
    let primero = a[indice];
    println!("El primer valor es: {}", primero);
}
